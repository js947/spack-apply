name: Test
on: [push, pull_request]

jobs:
  test:
    name: test1
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: run tests
        run: bash test/run.sh